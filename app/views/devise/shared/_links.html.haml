- if controller_name != 'sessions'
  = link_to "ログインの方はこちら", new_session_path(resource_name), class: 'Button'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "新規登録の方はこちら", new_registration_path(resource_name), class: 'Button'
  %br/

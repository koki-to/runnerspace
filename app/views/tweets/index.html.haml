.Home__global.img1
  .Home__global__section
    .Content
      = render 'search'
      .Articles
        #city-name
        #weather
        - @tweets.each do |tweet|
          = render 'tweet', tweet: tweet
    = paginate @tweets